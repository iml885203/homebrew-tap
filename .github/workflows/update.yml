name: Update formula

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:
    inputs:
      project:
        description: Formula to update
        required: true
        default: tunlease
        type: choice
        options: [tunlease, orbit]
      version:
        description: Exact released tag; defaults to the latest release
        required: false
        type: string
      correlation:
        description: Caller-provided run correlation
        required: false
        type: string

run-name: Update ${{ inputs.project || 'tunlease' }} ${{ inputs.version || 'latest' }} ${{ inputs.correlation }}

concurrency:
  group: update-${{ inputs.project || 'tunlease' }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  resolve:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      project: ${{ steps.release.outputs.project }}
      tag: ${{ steps.release.outputs.tag }}
      version: ${{ steps.release.outputs.version }}
      current: ${{ steps.release.outputs.current }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Resolve release
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
          REQUESTED_PROJECT: ${{ inputs.project }}
          REQUESTED_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          project="${REQUESTED_PROJECT:-tunlease}"
          case "$project" in tunlease|orbit) ;; *) echo "unsupported project: $project" >&2; exit 1;; esac
          tag="$REQUESTED_VERSION"
          if [ -z "$tag" ]; then
            tag=$(gh api "repos/iml885203/$project/releases/latest" --jq .tag_name)
          fi
          if ! [[ "$tag" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "invalid release tag: $tag" >&2
            exit 1
          fi
          version=${tag#v}
          if [ "$project" = orbit ]; then
            current=$(sed -nE 's|.*releases/download/v([^/]+)/orbit-.*|\1|p' Formula/orbit.rb | head -n 1)
          else
            current=$(sed -nE 's|.*tags/v([^/]+)\.tar\.gz.*|\1|p' Formula/tunlease.rb)
          fi
          echo "project=$project" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          python3 .github/scripts/semver-not-older.py "$current" "$version"
          if [ "$version" = "$current" ]; then echo "current=true" >> "$GITHUB_OUTPUT"; fi

  verify-orbit:
    needs: resolve
    if: needs.resolve.outputs.project == 'orbit'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Verify immutable Orbit release
        uses: iml885203/orbit/.github/actions/verify-orbit-release@d7f2c503228479f27162e558720a6e679627f809 # v0.16.0 release-security boundary
        env:
          GH_TOKEN: ${{ github.token }}
        with:
          mode: published
          tag: ${{ needs.resolve.outputs.tag }}

  update:
    needs: [resolve, verify-orbit]
    if: >-
      always() &&
      needs.resolve.result == 'success' &&
      needs.resolve.outputs.current != 'true' &&
      (needs.resolve.outputs.project != 'orbit' || needs.verify-orbit.result == 'success')
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Update Tunlease formula
        if: needs.resolve.outputs.project == 'tunlease'
        env:
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          url="https://github.com/iml885203/tunlease/archive/refs/tags/${TAG}.tar.gz"
          sha=$(curl -fsSL "$url" | sha256sum | cut -d' ' -f1)
          sed -i -E "s|url \".*\"|url \"${url}\"|" Formula/tunlease.rb
          sed -i -E "s|sha256 \".*\"|sha256 \"${sha}\"|" Formula/tunlease.rb
      - name: Update Orbit formula
        if: needs.resolve.outputs.project == 'orbit'
        env:
          TAG: ${{ needs.resolve.outputs.tag }}
          VERSION: ${{ needs.resolve.outputs.version }}
        run: |
          set -euo pipefail
          curl -fsSL "https://github.com/iml885203/orbit/releases/download/${TAG}/checksums.txt" -o checksums.txt
          for platform in darwin-amd64 darwin-arm64 linux-amd64 linux-arm64; do
            asset="orbit-${platform}"
            sha=$(awk -v name="$asset" '$2 == name || $2 == "*" name { print $1; exit }' checksums.txt)
            [ -n "$sha" ] || { echo "checksum missing for $asset" >&2; exit 1; }
            sed -i -E "s|releases/download/v[^/]+/${asset}|releases/download/${TAG}/${asset}|" Formula/orbit.rb
            sed -i -E "/${asset}/,/sha256/ s/sha256 \"[0-9a-f]+\"/sha256 \"${sha}\"/" Formula/orbit.rb
          done
          rm checksums.txt
      - name: Open, test, and merge update
        env:
          GH_TOKEN: ${{ github.token }}
          PROJECT: ${{ needs.resolve.outputs.project }}
          VERSION: ${{ needs.resolve.outputs.version }}
        run: |
          set -euo pipefail
          branch="automation/${PROJECT}-${VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B "$branch"
          git add "Formula/${PROJECT}.rb"
          git commit -m "${PROJECT} ${VERSION}"
          git push --force origin "$branch"
          if ! gh pr view "$branch" >/dev/null 2>&1; then
            gh pr create --base main --head "$branch" --title "${PROJECT} ${VERSION}" \
              --body "Automated formula update for ${PROJECT} ${VERSION}."
          fi
          previous_run_id=$(gh run list --workflow test.yml --branch "$branch" --event workflow_dispatch \
            --limit 1 --json databaseId --jq '.[0].databaseId')
          gh workflow run test.yml --ref "$branch"
          for _ in $(seq 1 20); do
            run_id=$(gh run list --workflow test.yml --branch "$branch" --event workflow_dispatch \
              --limit 1 --json databaseId --jq '.[0].databaseId')
            [ -n "$run_id" ] && [ "$run_id" != "$previous_run_id" ] && break
            run_id=
            sleep 3
          done
          [ -n "${run_id:-}" ] || { echo "test workflow did not start" >&2; exit 1; }
          gh run watch "$run_id" --exit-status
          gh pr merge "$branch" --squash --delete-branch
